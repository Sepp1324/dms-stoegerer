# Basis-Deployment (ohne NFS-Volume).
# Anwenden mit:  kubectl apply -k deploy/k8s
#
# Für NFS-Consume-Integration (Scanner-NAS → Worker):
#   kubectl apply -k deploy/k8s/overlays/consume-nfs
# (siehe overlays/consume-nfs/kustomization.yaml)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
