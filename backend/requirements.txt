# Django 5.2 ist LTS (Sicherheits-Support bis 2028); die 5.1-Serie ist per
# Kalender EOL – deshalb Wechsel auf 5.2. DRF 3.16 + simplejwt 5.5 tragen die
# offizielle Django-5.2-Unterstützung; cors-headers 4.7 ebenso.
Django==5.2.6
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.0
django-cors-headers==4.7.0
whitenoise==6.8.2
psycopg[binary]==3.2.3
celery==5.4.0
redis==5.2.1
python-dotenv==1.0.1
gunicorn==23.0.0
# Symmetrische Verschlüsselung für DB-Geheimnisse (MailAccount.password, STOAA-212).
# Innerhalb 44.x gehalten (Sicherheits-Patch); ein Major-Sprung zöge neue
# Rust/ABI-Anforderungen nach sich – bewusst separat.
cryptography==44.0.1
# OCR-Pipeline
ocrmypdf==16.10.0
img2pdf==0.6.0
PyMuPDF==1.24.9
# pikepdf explizit auf 8.x pinnen: ocrmypdf 16.10 ruft noch Pdf.check() auf,
# das in pikepdf 9.x entfernt wurde (sonst AttributeError → OCR schlägt fehl).
pikepdf==8.15.1
# Miniaturbilder (erste Seite → JPEG)
pdf2image==1.17.0
# Pillow zurück auf 10.4.0 (Rollback von #242): fastembed 0.8.0 lud sein ONNX-Modell
# über hf-xet nicht (model.onnx fehlte im Cache → semantische Suche fiel aus). Der
# 0.3.6-Cache liegt bereits im PVC → lädt ohne Download. Pillow bleibt damit an der
# fastembed-0.3.6-Decke (pillow<11); der Pillow-CVE-Fix ist auf einen späteren
# Anlauf verschoben (Modell vorab ins Image backen / Egress zum Xet-CDN klären).
Pillow==10.4.0
# HEIC/HEIF-Support für die Mobile-Erfassung (STOAA-513); die Wheels bündeln
# libheif → i. d. R. kein apt-Paket nötig. Image-Rebuild erforderlich.
pillow-heif==0.18.0
# QR-Codes für die Archivnummer (ASN, STOAA-284/285); rendert als PNG via Pillow
qrcode==7.4.2
# ASN-Barcode-Erkennung (Code128 + QR) – braucht libzbar0 als System-Lib (STOAA-515)
pyzbar==0.1.9

# AI-Anbindung (optional je nach Provider)
anthropic==0.42.0
httpx==0.28.1
# Semantische Suche: pgvector-Django-Integration (VectorField + VectorExtension).
# Braucht das pgvector-Postgres-Image (deploy/k8s/base/postgres.yaml). Leichtes
# Paket – keine ML-Abhängigkeit (die Embedding-Erzeugung kommt in Etappe 2).
pgvector==0.3.6
# Lokale Text-Embeddings für die semantische Suche: fastembed (ONNX-Runtime,
# mehrsprachig) – KEIN torch, läuft auf CPU. Modelle werden lazy geladen und
# unter EMBEDDING_CACHE_DIR (/data/models, persistentes PVC) gecached.
# Rollback auf 0.3.6 (#242 zurückgerollt): 0.8.0 lädt das Modell über hf-xet in ein
# neues Cache-Layout und scheiterte im Cluster (model.onnx fehlte → Index leer).
# 0.3.6 nutzt das bereits im PVC gecachte Modell → sofort lauffähig, kein Download.
# Ein erneuter Upgrade-Anlauf braucht das 0.8.0-Modell vorab im Image (kein
# Laufzeit-Download) bzw. gesicherten Egress zum HF-Xet-CDN. Siehe docs/embedding-upgrade.md.
fastembed==0.3.6
